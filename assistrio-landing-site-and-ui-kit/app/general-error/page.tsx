import ErrorPage from "@/components/ErrorPage";

export default function GeneralErrorDemo() {
    return <ErrorPage type="error" message="This is a demonstration of the general error page. Something unexpected happened and we're here to help." />;
}

